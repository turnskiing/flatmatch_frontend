import { makeStyles } from "@material-ui/core/styles"

export const DisplayOffersStyle = makeStyles((theme) => ({
	gridContainer: {
		paddingLeft: "15px",
		paddingRight: "15px",
		paddingTop: "20px",
	}
}))